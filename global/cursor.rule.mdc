---
description: Global agentic-coding preferences (versioned in the methodology repo)
alwaysApply: true
---

Read and follow the versioned file:

`<methodology-root>/global/AGENTS.md`

Replace `<methodology-root>` with this machine's clone path. If
`AGENTIC_METHODOLOGY_ROOT` is set, use that path instead.

## Personal (private — never committed anywhere)

- My name is `<your name>`.
- facts about this machine that don't belong in a public repo
